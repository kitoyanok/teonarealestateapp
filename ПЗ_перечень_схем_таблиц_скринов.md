# Перечень схем, диаграмм, таблиц и скриншотов для ПЗ

Данный файл является подробной рабочей инструкцией по сборке графической и табличной части пояснительной записки по проекту `Тэона`. Он нужен для того, чтобы без догадок понять:

- какие диаграммы нужно рисовать;
- какие актеры, сущности, связи и поля в них включать;
- какие таблицы должны быть в ПЗ;
- как должны выглядеть таблицы по базе данных;
- какие скриншоты нужно снять и что на них обязательно показать.

Файл синхронизирован с текущей структурой `Диплом_структура_ПЗ.md`, с отдельным файлом приложений `Приложения_ПЗ.md` и с реальным кодом проекта.

---

# 0 Общие правила оформления по образцу Петруновского

## 0.1 Как оформлять рисунки

Для каждого рисунка в основном тексте ПЗ используется одна и та же схема подачи:

1. короткий вводный абзац перед рисунком;
2. подпись рисунка под изображением;
3. поясняющий абзац после рисунка со словами `В соответствии с рисунком ...`.

### Шаблон текста перед рисунком

`На рисунке N представлена ...`

### Шаблон подписи рисунка

`Рисунок N – ...`

### Шаблон текста после рисунка

`В соответствии с рисунком N, ...`

## 0.2 Как оформлять таблицы

Перед таблицей в основном тексте должен быть короткий вводный абзац. Формулировка должна звучать как готовый текст ПЗ, а не как указание на будущее действие.

### Шаблон текста перед таблицей

`Результаты сравнения представлены в таблице N.`

или

`Более детальное описание ... представлено в таблице N.`

Подпись таблицы ставится над таблицей. Формат подписи:

`Таблица N – ...`

Если таблица находится в приложении, используется буквенная нумерация:

- `Таблица А.1`;
- `Таблица В.1`;
- `Таблица Г.1`;
- `Таблица Д.1`;
- `Таблица Е.1`.

Если таблица переносится на следующую страницу, используется подпись вида:

`Продолжение таблицы N`

После таблицы желательно добавить один короткий абзац с выводом:

`Как видно из таблицы N, ...`

или

`На основании данных таблицы N ...`

## 0.3 Что важно не выдумывать

Диаграммы и схемы должны отражать реальную текущую систему, а не предполагаемую. Для проекта `Тэона` это означает следующее:

- отдельного экрана просмотра истории поисковых запусков сейчас нет;
- история запусков **хранится в БД** и используется в аналитике, но не выделена в отдельный пользовательский экран;
- поэтому на use case диаграммах не нужно рисовать отдельный прецедент `Просмотреть историю запусков поиска` как самостоятельную экранную функцию;
- `search_runs` нужно показывать в ER-диаграмме и в таблицах БД, потому что таблица реально существует и используется backend-логикой.

## 0.4 Где размещать формулы

Формулы уместнее всего размещать в разделе алгоритмов:

- `2.6.2 Разработка алгоритма нормализации объявления`;
- `2.6.4 Разработка алгоритма расчета процента совпадения`.

Для проекта `Тэона` следует использовать:

- формулу расчета стоимости одного квадратного метра;
- формулу итогового процента совпадения;
- формулу коэффициента совпадения по коммуникациям.

## 0.5 Что изменено после сверки с последней версией референса

После сверки с файлом `Диплом ПЗ — копия — копия3.docx` структура основных диаграмм не менялась: для проекта `Тэона` остаются рисунки `1–27` и кодовые рисунки `В.1–В.4`. Изменения касаются оформления:

- в основном тексте перед таблицами и рисунками используются завершенные формулировки `представлена`, `приведена`, `отражена`;
- в приложении А оставлены таблицы `А.1–А.4`, как в новой версии референса;
- в приложении Г добавлена таблица `Г.1 – Общая информация о тестировании`;
- тест-кейсы приложения Г перенумерованы как `Г.2–Г.4`;
- отдельный рисунок `Г.1` не используется, потому что результат модульных тестов уже представлен в основном тексте на рисунке 27.

---

# 1 Диаграммы и схемы основного текста

## 1.1 Рисунок 1 – Общая диаграмма вариантов использования программных модулей системы «Тэона»

### Что это за диаграмма

Это верхнеуровневая `use case` диаграмма. Она показывает, какие основные действия выполняет риелтор в системе.

### Какого актера рисовать

Основной актер один:

- `Риелтор`.

Внешние сайты недвижимости здесь не нужно делать отдельным актером. Их правильнее показать на диаграмме компонентов.

### Какие прецеденты рисовать

- `Авторизоваться в системе`;
- `Просмотреть dashboard`;
- `Создать карточку клиента`;
- `Изменить карточку клиента`;
- `Запустить поиск объектов`;
- `Просмотреть найденные объекты`;
- `Открыть подробности объекта`;
- `Добавить объект в подборку`;
- `Удалить объект из подборки`;
- `Сформировать текст сообщения`;
- `Отметить подборку как отправленную`.

### Какие связи рисовать

- `Создать карточку клиента` <<include>> `Запустить поиск объектов`;
- `Просмотреть найденные объекты` <<extend>> `Открыть подробности объекта`;
- `Добавить объект в подборку` <<include>> `Сформировать текст сообщения`;
- `Сформировать текст сообщения` <<extend>> `Отметить подборку как отправленную`.

### Как расположить

- риелтор слева;
- внутри границы системы все use case;
- сверху разместить авторизацию и dashboard;
- в центре карточку клиента и запуск поиска;
- справа работу с найденными объектами;
- снизу подборку и отправку.

### Что не надо рисовать

- отдельный прецедент `Просмотреть историю запусков поиска`;
- технические прецеденты уровня `Сохранить в PostgreSQL`, `Обратиться к API`, `Обработать HTML`.

## 1.2 Рисунок 2 – Диаграмма вариантов использования модуля создания и ведения клиентских заявок

### Что должно быть на диаграмме

- `Создать карточку клиента`;
- `Изменить карточку клиента`;
- `Сохранить профиль поиска`;
- `Запустить поиск объектов`;
- `Открыть карточку клиента`;
- `Повторно запустить поиск`.

### Связи

- `Создать карточку клиента` <<include>> `Сохранить профиль поиска`;
- `Создать карточку клиента` <<include>> `Запустить поиск объектов`;
- `Открыть карточку клиента` <<extend>> `Повторно запустить поиск`.

### Важная оговорка

История запусков поиска в текущем интерфейсе не выделена отдельным самостоятельным экраном. Поэтому ее не нужно рисовать как отдельный use case. Если захочешь показать ее косвенно, это можно сделать только как внутренний результат работы карточки клиента, а не как отдельное пользовательское действие.

## 1.3 Рисунок 3 – Диаграмма вариантов использования модуля поиска объектов недвижимости

### Что должно быть на диаграмме

- `Запустить поиск объектов`;
- `Отфильтровать объекты по городу Краснодару`;
- `Нормализовать найденные объявления`;
- `Рассчитать процент совпадения`;
- `Сохранить результаты поиска`;
- `Просмотреть найденные объекты`.

### Как строить

- риелтор связывается только с `Запустить поиск объектов` и `Просмотреть найденные объекты`;
- остальные действия включаются в `Запустить поиск объектов` через <<include>>.

### Что важно подчеркнуть

Это модуль автоматической обработки. Пользователь не управляет вручную фильтрацией по Краснодару и расчетом `matchScore`, он только запускает поиск и получает результат.

## 1.4 Рисунок 4 – Диаграмма вариантов использования модуля формирования подборки объектов

### Что должно быть на диаграмме

- `Открыть карточку клиента`;
- `Просмотреть найденные объекты`;
- `Добавить объект в подборку`;
- `Удалить объект из подборки`;
- `Сформировать текст сообщения`;
- `Отметить подборку как отправленную`.

### Что важно подчеркнуть

Подборка формируется вручную. Это не автоматический рейтинг и не автоподбор без участия риелтора.

## 1.5 Рисунок 5 – Диаграмма вариантов использования модуля аналитики и контроля состояния заявок

### Что должно быть на диаграмме

- `Просмотреть dashboard`;
- `Просмотреть число клиентов в работе`;
- `Просмотреть число найденных объектов`;
- `Просмотреть число объектов в подборках`;
- `Просмотреть число подборок, готовых к отправке`;
- `Перейти к заявкам, требующим внимания`.

### Что не надо рисовать

- отдельный прецедент `Открыть историю запусков поиска`;
- отдельный прецедент `Строить график` как действие пользователя.

График активности — это часть dashboard, а не отдельная функция пользователя.

## 1.6 Рисунок 6 – Диаграмма деятельности подбора объектов недвижимости по профилю клиента

### Блоки по порядку

1. `Начало`.
2. `Открытие формы создания клиента`.
3. `Ввод данных клиента`.
4. `Сохранение карточки клиента`.
5. `Сохранение профиля поиска`.
6. `Создание search_run`.
7. `Запуск search-service`.
8. `Выбор источников`.
9. `Загрузка страниц`.
10. `Извлечение карточек объектов`.
11. `Проверка обязательных данных`.
12. `Проверка принадлежности к Краснодару`.
13. `Нормализация характеристик`.
14. `Расчет процента совпадения`.
15. `Сохранение properties`.
16. `Сохранение client_found_properties`.
17. `Обновление статуса клиента`.
18. `Отображение найденных объектов`.
19. `Конец`.

### Ромбы-условия

- `Объект извлечен корректно?`;
- `Объект относится к Краснодару?`;
- `Найден хотя бы один объект?`;
- `Поиск завершился ошибкой?`.

## 1.7 Рисунок 7 – Диаграмма состояний клиентской заявки

### Состояния

- `new`;
- `searching`;
- `found`;
- `no_results`;
- `shortlist_ready`;
- `sent`;
- `error`.

### Переходы

- `new -> searching` после сохранения карточки и старта поиска;
- `searching -> found` если объекты найдены;
- `searching -> no_results` если поиск завершился без результатов;
- `searching -> error` если поиск завершился ошибкой;
- `found -> shortlist_ready` после добавления хотя бы одного объекта в подборку;
- `shortlist_ready -> found` если подборка снова стала пустой, но найденные объекты остались;
- `shortlist_ready -> sent` после отметки отправки подборки.

### Что не надо дорисовывать

- `closed`, если такого статуса нет в текущем UI и API;
- произвольные переходы `error -> sent`, `no_results -> sent`.

## 1.8 Рисунок 8 – Диаграмма компонентов программной системы «Тэона»

### Какие компоненты рисовать

- `Web Client`;
- `API Server`;
- `Search Service`;
- `PostgreSQL`;
- `External Real Estate Websites`.

### Что написать внутри

**Web Client**

- `Login`;
- `Dashboard`;
- `Clients`;
- `Client Card`;
- `Property Drawer`;
- `Shortlist`.

**API Server**

- `Auth`;
- `Clients`;
- `Dashboard`;
- `Search Launch`;
- `Shortlist`;
- `Message Builder`.

**Search Service**

- `Sources`;
- `Base Adapter`;
- `Normalizer`;
- `Matcher`.

**PostgreSQL**

- `users`;
- `clients`;
- `client_search_profiles`;
- `properties`;
- `client_found_properties`;
- `shortlist_items`;
- `share_messages`;
- `search_runs`.

### Какие связи рисовать

- `Web Client -> API Server`;
- `API Server -> PostgreSQL`;
- `API Server -> Search Service`;
- `Search Service -> External Real Estate Websites`;
- `Search Service -> API Server` можно показать обратной стрелкой как возврат результатов;
- `API Server -> Web Client` можно не дублировать, если связь уже понятна по двусторонней стрелке.

## 1.9 Рисунки 9–10 – ER-диаграмма базы данных

Ниже приведены точные сущности, связи и поля, которые нужно использовать в ER-диаграмме.

### Общий список сущностей

- `users`;
- `clients`;
- `client_search_profiles`;
- `properties`;
- `client_found_properties`;
- `shortlist_items`;
- `share_messages`;
- `search_runs`.

### Общий список связей

- `users.id -> clients.realtor_id` (`1:M`);
- `clients.id -> client_search_profiles.client_id` (`1:1`);
- `clients.id -> client_found_properties.client_id` (`1:M`);
- `properties.id -> client_found_properties.property_id` (`1:M`);
- `clients.id -> shortlist_items.client_id` (`1:M`);
- `properties.id -> shortlist_items.property_id` (`1:M`);
- `clients.id -> share_messages.client_id` (`1:M`);
- `clients.id -> search_runs.client_id` (`1:M`).

### Рисунок 9 – Фрагмент ER-диаграммы базы данных: пользователи системы, карточки клиентов, профили поиска и история запусков

**Размещение**

- `users` слева;
- `clients` по центру;
- `client_search_profiles` справа.
- `search_runs` в нижней правой части фрагмента.

**Кардинальности**

- `users (1) -> (M) clients`;
- `clients (1) -> (1) client_search_profiles`.
- `clients (1) -> (M) search_runs`.

**Какие поля показывать в блоках**

**users**

- `id`;
- `login`;
- `password_hash`;
- `name`;
- `email`;
- `phone`;
- `created_at`;
- `updated_at`.

**clients**

- `id`;
- `realtor_id`;
- `name`;
- `phone`;
- `email`;
- `send_channel`;
- `send_contact`;
- `status`;
- `property_type`;
- `comment`;
- `created_at`;
- `updated_at`.

**client_search_profiles**

- `id`;
- `client_id`;
- `budget_min`;
- `budget_max`;
- `rooms_min`;
- `rooms_max`;
- `area_min`;
- `area_max`;
- `districts`;
- `settlement_names`;
- `completion_year_min`;
- `completion_year_max`;
- `finishing`;
- `floor_min`;
- `floor_max`;
- `house_area_min`;
- `house_area_max`;
- `land_area_min`;
- `land_area_max`;
- `floors_count_min`;
- `floors_count_max`;
- `bedrooms_min`;
- `bedrooms_max`;
- `house_material`;
- `communications`;
- `created_at`;
- `updated_at`.

**search_runs**

- `id`;
- `client_id`;
- `status`;
- `property_type`;
- `total_found`;
- `total_saved`;
- `error_message`;
- `started_at`;
- `finished_at`.

### Рисунок 10 – Фрагмент ER-диаграммы базы данных: объекты недвижимости, результаты поиска, подборки и сообщения

**Размещение**

- `clients` слева;
- `properties` по центру;
- `client_found_properties` справа от `properties`;
- `shortlist_items` еще правее `client_found_properties`;
- `share_messages` ниже `client_found_properties`;
- `clients` можно оставить укороченным блоком слева как связующую сущность.

**Какие поля показывать в блоках**

**properties**

- `id`;
- `external_id`;
- `source_name`;
- `source_url`;
- `property_type`;
- `title`;
- `complex_name`;
- `developer_name`;
- `description`;
- `city`;
- `district`;
- `address`;
- `settlement_name`;
- `price`;
- `price_per_meter`;
- `area`;
- `rooms`;
- `floor`;
- `floors_total`;
- `house_area`;
- `land_area`;
- `bedrooms`;
- `house_floors`;
- `house_material`;
- `communications`;
- `completion_year`;
- `finishing`;
- `images`;
- `raw_data`;
- `first_seen_at`;
- `last_seen_at`.

**client_found_properties**

- `id`;
- `client_id`;
- `property_id`;
- `match_score`;
- `match_reasons`;
- `mismatch_reasons`;
- `is_hidden`;
- `created_at`.

**shortlist_items**

- `id`;
- `client_id`;
- `property_id`;
- `note`;
- `created_at`.

**share_messages**

- `id`;
- `client_id`;
- `channel`;
- `contact`;
- `message_text`;
- `copied_at`;
- `sent_marked_at`;
- `created_at`.

### Что обязательно показать в поясняющем тексте под рисунками 9–10

- таблица `clients` является центральной сущностью пользовательского контура;
- таблица `search_runs` нужна не как формальность, а для хранения истории запусков поиска, количества найденных и сохраненных объектов, а также текста ошибки;
- таблица `properties` хранит единый нормализованный каталог объектов без привязки к одному клиенту;
- таблица `client_found_properties` реализует связь конкретного клиента с конкретным найденным объектом и хранит `match_score`, `match_reasons`, `mismatch_reasons`;
- таблица `shortlist_items` отражает ручной выбор риелтора;
- таблица `share_messages` хранит уже подготовленные тексты сообщений и отметку об отправке.

**search_runs**

- `id`;
- `client_id`;
- `status`;
- `property_type`;
- `total_found`;
- `total_saved`;
- `error_message`;
- `started_at`;
- `finished_at`.

---

# 2 Полный состав таблиц базы данных

Ниже приведены таблицы БД так, как их нужно понимать для ПЗ. Это не SQL, а удобная расшифровка всех полей.

## 2.1 Таблица `users`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор риелтора | Да |
| `login` | VARCHAR(120) | Логин для входа | Да |
| `password_hash` | TEXT | Хэш пароля | Да |
| `name` | VARCHAR(160) | Имя риелтора | Да |
| `email` | VARCHAR(160) | Электронная почта | Нет |
| `phone` | VARCHAR(40) | Телефон | Нет |
| `created_at` | TIMESTAMP | Дата создания записи | Да |
| `updated_at` | TIMESTAMP | Дата последнего изменения | Да |

## 2.2 Таблица `clients`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор клиента | Да |
| `realtor_id` | UUID | Ссылка на риелтора | Да |
| `name` | VARCHAR(160) | ФИО клиента | Да |
| `phone` | VARCHAR(40) | Телефон клиента | Да |
| `email` | VARCHAR(160) | Электронная почта клиента | Нет |
| `send_channel` | VARCHAR(40) | Канал отправки подборки | Да |
| `send_contact` | VARCHAR(200) | Контакт для отправки | Нет |
| `status` | VARCHAR(40) | Статус заявки | Да |
| `property_type` | VARCHAR(20) | Тип недвижимости | Да |
| `comment` | TEXT | Комментарий риелтора | Нет |
| `created_at` | TIMESTAMP | Дата создания | Да |
| `updated_at` | TIMESTAMP | Дата изменения | Да |

## 2.3 Таблица `client_search_profiles`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор профиля поиска | Да |
| `client_id` | UUID | Ссылка на клиента | Да |
| `budget_min` | NUMERIC | Нижняя граница бюджета | Нет |
| `budget_max` | NUMERIC | Верхняя граница бюджета | Нет |
| `rooms_min` | INTEGER | Минимум комнат | Нет |
| `rooms_max` | INTEGER | Максимум комнат | Нет |
| `area_min` | NUMERIC | Минимальная площадь квартиры | Нет |
| `area_max` | NUMERIC | Максимальная площадь квартиры | Нет |
| `districts` | TEXT[] | Районы Краснодара | Да |
| `settlement_names` | TEXT[] | Локации для домов | Да |
| `completion_year_min` | INTEGER | Минимальный год сдачи | Нет |
| `completion_year_max` | INTEGER | Максимальный год сдачи | Нет |
| `finishing` | VARCHAR(80) | Тип отделки | Нет |
| `floor_min` | INTEGER | Минимальный этаж | Нет |
| `floor_max` | INTEGER | Максимальный этаж | Нет |
| `house_area_min` | NUMERIC | Минимальная площадь дома | Нет |
| `house_area_max` | NUMERIC | Максимальная площадь дома | Нет |
| `land_area_min` | NUMERIC | Минимальная площадь участка | Нет |
| `land_area_max` | NUMERIC | Максимальная площадь участка | Нет |
| `floors_count_min` | INTEGER | Минимум этажей дома | Нет |
| `floors_count_max` | INTEGER | Максимум этажей дома | Нет |
| `bedrooms_min` | INTEGER | Минимум спален | Нет |
| `bedrooms_max` | INTEGER | Максимум спален | Нет |
| `house_material` | VARCHAR(120) | Материал дома | Нет |
| `communications` | TEXT[] | Коммуникации | Да |
| `created_at` | TIMESTAMP | Дата создания | Да |
| `updated_at` | TIMESTAMP | Дата изменения | Да |

## 2.4 Таблица `properties`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор объекта | Да |
| `external_id` | TEXT | Внешний идентификатор источника | Нет |
| `source_name` | VARCHAR(160) | Название источника | Нет |
| `source_url` | TEXT | Ссылка на объявление | Да |
| `property_type` | VARCHAR(20) | Тип объекта | Да |
| `title` | TEXT | Нормализованное название | Да |
| `complex_name` | VARCHAR(240) | Название ЖК или поселка | Нет |
| `developer_name` | VARCHAR(240) | Застройщик | Нет |
| `description` | TEXT | Описание объекта | Нет |
| `city` | VARCHAR(120) | Город | Нет |
| `district` | VARCHAR(160) | Район | Нет |
| `address` | TEXT | Адрес | Нет |
| `settlement_name` | VARCHAR(200) | Населенный пункт / поселок | Нет |
| `price` | NUMERIC | Цена | Нет |
| `price_per_meter` | NUMERIC | Цена за м² | Нет |
| `area` | NUMERIC | Площадь квартиры | Нет |
| `rooms` | INTEGER | Количество комнат | Нет |
| `floor` | INTEGER | Этаж | Нет |
| `floors_total` | INTEGER | Этажность дома | Нет |
| `house_area` | NUMERIC | Площадь дома | Нет |
| `land_area` | NUMERIC | Площадь участка | Нет |
| `bedrooms` | INTEGER | Число спален | Нет |
| `house_floors` | INTEGER | Число этажей дома | Нет |
| `house_material` | VARCHAR(120) | Материал дома | Нет |
| `communications` | TEXT[] | Коммуникации | Да |
| `completion_year` | INTEGER | Год сдачи | Нет |
| `finishing` | VARCHAR(120) | Отделка | Нет |
| `images` | TEXT[] | Ссылки на изображения | Да |
| `raw_data` | JSONB | Сырой объект источника | Нет |
| `first_seen_at` | TIMESTAMP | Первый момент обнаружения | Да |
| `last_seen_at` | TIMESTAMP | Последний момент обнаружения | Да |

## 2.5 Таблица `client_found_properties`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор записи результата | Да |
| `client_id` | UUID | Ссылка на клиента | Да |
| `property_id` | UUID | Ссылка на объект | Да |
| `match_score` | INTEGER | Процент совпадения | Нет |
| `match_reasons` | TEXT[] | Причины совпадения | Да |
| `mismatch_reasons` | TEXT[] | Причины несоответствия | Да |
| `is_hidden` | BOOLEAN | Скрыт ли результат | Да |
| `created_at` | TIMESTAMP | Дата сохранения результата | Да |

## 2.6 Таблица `shortlist_items`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор записи подборки | Да |
| `client_id` | UUID | Ссылка на клиента | Да |
| `property_id` | UUID | Ссылка на объект | Да |
| `note` | TEXT | Комментарий к объекту | Нет |
| `created_at` | TIMESTAMP | Дата добавления в подборку | Да |

## 2.7 Таблица `share_messages`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор сообщения | Да |
| `client_id` | UUID | Ссылка на клиента | Да |
| `channel` | VARCHAR(40) | Канал отправки | Да |
| `contact` | VARCHAR(200) | Контакт клиента | Нет |
| `message_text` | TEXT | Сформированный текст | Да |
| `copied_at` | TIMESTAMP | Время копирования текста | Нет |
| `sent_marked_at` | TIMESTAMP | Время отметки отправки | Нет |
| `created_at` | TIMESTAMP | Время создания сообщения | Да |

## 2.8 Таблица `search_runs`

| Поле | Тип | Назначение | Обязательность |
|---|---|---|---|
| `id` | UUID | Идентификатор запуска поиска | Да |
| `client_id` | UUID | Ссылка на клиента | Да |
| `status` | VARCHAR(40) | Статус запуска (`running`, `ok`, `error`) | Да |
| `property_type` | VARCHAR(20) | Тип поиска | Да |
| `total_found` | INTEGER | Сколько объектов найдено | Да |
| `total_saved` | INTEGER | Сколько объектов сохранено | Да |
| `error_message` | TEXT | Текст ошибки | Нет |
| `started_at` | TIMESTAMP | Время старта | Да |
| `finished_at` | TIMESTAMP | Время завершения | Нет |

## 2.9 Какие таблицы обязательно показать на скриншотах

Минимальный набор для скриншотов:

- `users`;
- `clients`;
- `client_search_profiles`;
- `properties`;
- `client_found_properties`;
- `shortlist_items`;
- `share_messages`;
- `search_runs`.

Если на одном скриншоте всё не помещается, делай два:

1. `users`, `clients`, `client_search_profiles`, `search_runs`;
2. `properties`, `client_found_properties`, `shortlist_items`, `share_messages`.

---

# 3 Все таблицы, которые должны быть в ПЗ

Ниже приведены не просто названия таблиц, а содержание каждой таблицы: какие графы в ней должны быть и что именно в них писать.

## 3.1 Таблицы сценариев основного текста

### Таблица 1 – Главный раздел сценария варианта использования «Создание карточки клиента и запуск поиска»

**Столбцы:** `Поле | Содержание`

**Строки:**

- Вариант использования (прецедент);
- Актеры;
- Краткое описание;
- Цель;
- Тип;
- Предусловие;
- Постусловие;
- Основной результат.

### Таблица 2 – Сценарий успешного выполнения варианта использования «Создание карточки клиента и запуск поиска»

**Столбцы:** `Действия актера | Отклик системы`

**Содержание:** пошаговый сценарий с действиями `Действие №1`, `Действие №2` и так далее. Внутри этой таблицы допускаются отдельные строки `Исключение №...`, как в исправленной версии Петруновского.

### Таблица 3 – Обработка исключительных ситуаций для варианта использования «Создание карточки клиента и запуск поиска»

**Столбцы:** `Действия актера | Отклик системы`

**Что должно быть внутри:**

- не заполнены обязательные поля;
- внешний модуль поиска недоступен;
- по параметрам клиента не найдено ни одного объекта.

### Таблица 4 – Главный раздел сценария варианта использования «Формирование подборки объектов для клиента»

**Столбцы:** `Поле | Содержание`

**Строки:** те же, что в таблице 1.

### Таблица 5 – Сценарий успешного выполнения варианта использования «Формирование подборки объектов для клиента»

**Столбцы:** `Действия актера | Отклик системы`

**Что должно быть внутри:**

- открытие карточки клиента;
- просмотр найденных объектов;
- добавление объекта в подборку;
- формирование текста сообщения;
- отметка отправки.

### Таблица 6 – Обработка исключительных ситуаций для варианта использования «Формирование подборки объектов для клиента»

**Столбцы:** `Действия актера | Отклик системы`

**Что должно быть внутри:**

- объект уже недоступен;
- подборка пуста;
- текст сообщения не удалось сформировать.

## 3.2 Таблицы проектирования и реализации

### Таблица 7 – Характеристика сущностей и связей между сущностями ER-диаграммы

**Столбцы:** `Сущность | Назначение сущности | Ключ | Связь`

**Строки:**

- `users`;
- `clients`;
- `client_search_profiles`;
- `properties`;
- `client_found_properties`;
- `shortlist_items`;
- `share_messages`;
- `search_runs`.

Для каждой строки нужно коротко объяснить назначение сущности, указать первичный ключ и текстом описать связь с другими таблицами.

### Таблица 8 – Сравнение языков программирования

**Столбцы:** `Язык | Преимущества | Недостатки | Вывод`

**Строки:**

- `TypeScript`;
- `Python`;
- `C#`.

### Таблица 9 – Сравнение средств разработки пользовательского интерфейса

**Столбцы:** `Средство | Преимущества | Недостатки | Вывод`

**Строки:**

- `React + Vite`;
- `Angular`;
- `Vue`.

### Таблица 10 – Сравнение систем управления базами данных

**Столбцы:** `СУБД | Преимущества | Недостатки | Вывод`

**Строки:**

- `PostgreSQL`;
- `Microsoft SQL Server`;
- `MySQL`.

### Таблица 11 – Используемые средства разработки и сопровождения

**Столбцы:** `Средство | Назначение | Причина использования`

**Строки:**

- `Visual Studio Code`;
- `Docker Desktop`;
- `TablePlus`;
- `PostgreSQL`.

### Таблица 12 – Характеристика основных таблиц базы данных

**Столбцы:** `Таблица | Поле | Тип данных | Значение NULL`

**Строки, которые нужно включить:**

- `users.id | UUID | Нет`
- `users.login | VARCHAR(120) | Нет`
- `users.password_hash | TEXT | Нет`
- `users.name | VARCHAR(160) | Нет`
- `users.email | VARCHAR(160) | Да`
- `users.phone | VARCHAR(40) | Да`
- `clients.id | UUID | Нет`
- `clients.realtor_id | UUID | Нет`
- `clients.name | VARCHAR(160) | Нет`
- `clients.phone | VARCHAR(40) | Нет`
- `clients.send_channel | VARCHAR(40) | Нет`
- `clients.send_contact | VARCHAR(200) | Да`
- `clients.status | VARCHAR(40) | Нет`
- `clients.property_type | VARCHAR(20) | Нет`
- `client_search_profiles.client_id | UUID | Нет`
- `client_search_profiles.budget_min | NUMERIC | Да`
- `client_search_profiles.budget_max | NUMERIC | Да`
- `client_search_profiles.districts | TEXT[] | Нет`
- `client_search_profiles.settlement_names | TEXT[] | Нет`
- `client_search_profiles.communications | TEXT[] | Нет`
- `properties.id | UUID | Нет`
- `properties.source_name | VARCHAR(160) | Да`
- `properties.source_url | TEXT | Нет`
- `properties.property_type | VARCHAR(20) | Нет`
- `properties.title | TEXT | Нет`
- `properties.district | VARCHAR(160) | Да`
- `properties.price | NUMERIC | Да`
- `properties.area | NUMERIC | Да`
- `properties.rooms | INTEGER | Да`
- `properties.images | TEXT[] | Нет`
- `client_found_properties.client_id | UUID | Нет`
- `client_found_properties.property_id | UUID | Нет`
- `client_found_properties.match_score | INTEGER | Да`
- `client_found_properties.match_reasons | TEXT[] | Нет`
- `client_found_properties.mismatch_reasons | TEXT[] | Нет`
- `shortlist_items.client_id | UUID | Нет`
- `shortlist_items.property_id | UUID | Нет`
- `shortlist_items.note | TEXT | Да`
- `shortlist_items.created_at | TIMESTAMP | Нет`
- `share_messages.client_id | UUID | Нет`
- `share_messages.channel | VARCHAR(40) | Нет`
- `share_messages.contact | VARCHAR(200) | Да`
- `share_messages.message_text | TEXT | Нет`
- `share_messages.sent_marked_at | TIMESTAMP | Да`
- `search_runs.client_id | UUID | Нет`
- `search_runs.status | VARCHAR(40) | Нет`
- `search_runs.property_type | VARCHAR(20) | Нет`
- `search_runs.total_found | INTEGER | Нет`
- `search_runs.total_saved | INTEGER | Нет`
- `search_runs.error_message | TEXT | Да`

### Таблица 13 – Основные программные модули и файлы реализации

**Столбцы:** `Модуль | Файлы реализации | Состав фрагментов приложения В`

**Строки:**

- модуль авторизации и ведения клиентских заявок;
- модуль поиска и обработки объявлений;
- модуль нормализации и ранжирования объявлений;
- модуль формирования подборки, сообщения и аналитики.

### Таблица 14 – Модульные тесты поискового модуля

**Столбцы:** `Файл теста | Проверяемая функция | Содержание проверки | Ожидаемый результат`

**Строки:**

- `test_normalization.py`;
- `test_matcher.py`.

### Таблица 15 – Основные интеграционные проверки

**Столбцы:** `Проверка | Что выполняется | Ожидаемый результат`

**Строки:**

- вход в систему;
- создание клиента;
- повторный запуск поиска;
- добавление объекта в подборку;
- формирование текста сообщения;
- отметка отправки.

### Таблица 16 – Основные команды запуска и сопровождения системы

**Столбцы:** `Команда | Назначение`

**Строки:**

- `npm run prod:up`;
- `npm run prod:down`;
- `npm run prod:logs`;
- `npm run prod:reset-db`;
- `cd apps/search-service && .venv/bin/python -m unittest discover -s tests -v`.

## 3.3 Таблицы приложений

### Приложение А

Должны быть таблицы:

- `А.1` — входные данные модуля авторизации и ведения клиентских заявок;
- `А.2` — выходные данные модуля авторизации и ведения клиентских заявок;
- `А.3` — входные данные модуля поиска и обработки объявлений;
- `А.4` — выходные данные модуля поиска и обработки объявлений.

В новой версии референса Петруновского входные и выходные данные подробно таблично раскрыты только для первых двух укрупненных модулей. Для проекта `Тэона` этот подход сохранен: остальные модули описаны через состав функций в подразделах `1.3` и `1.4`, а общие ограничения к данным вынесены в подраздел `1.5.3`.

**Формат всех таблиц А.1–А.4:**

`Наименование данных | Тип данных | Формат данных | Ограничения`

### Приложение В

Должны быть таблицы:

- `В.1` — функции модуля авторизации и ведения клиентских заявок;
- `В.2` — функции модуля поиска и обработки объявлений;
- `В.3` — функции модуля нормализации и ранжирования объявлений;
- `В.4` — функции модуля формирования подборки, сообщения и аналитики.

**Формат всех таблиц В.1–В.4:**

`Функция | Задача функции | Входные данные | Выходные данные`

### Приложение Г

Должны быть таблицы:

- `Г.1` — общая информация о тестировании;
- `Г.2` — интеграционный test case на создание клиента;
- `Г.3` — интеграционный test case на подборку и сообщение;
- `Г.4` — интеграционный test case на пустую подборку.

**Формат таблицы Г.1:**

`Название | Описание`

**Строки таблицы Г.1:**

- `Название проекта`;
- `Номер версии`;
- `Имя тестера`;
- `Даты тестирования`.

**Формат test case Г.2–Г.4:**

`Название | Описание`

**Строки в каждой таблице Г.2–Г.4:**

- `Test Case #`;
- `Приоритет теста`;
- `Название тестирования/Имя`;
- `Резюме испытания`;
- `Шаги тестирования`;
- `Данные тестирования`;
- `Ожидаемый результат`;
- `Фактический результат`;
- `Предпосылки`;
- `Постусловия`;
- `Статус (Pass/Fail)`;
- `Комментарии`.

### Приложение Д

Должна быть таблица:

- `Таблица Д.1 – Основные сообщения пользователю`.

**Формат:**

`Сообщение | Причина появления | Действие пользователя`

### Приложение Е

Должна быть таблица:

- `Таблица Е.1 – Основные служебные сообщения программы`.

**Формат:**

`Сообщение или ситуация | Возможная причина | Рекомендуемое действие`

---

# 4 Скриншоты и кодовые рисунки

## 4.1 Как запускать систему для скриншотов

```bash
npm run prod:up
```

После запуска проверить:

- web: `http://localhost:5002`;
- API health: `http://localhost:5003/api/health`;
- search-service health: `http://localhost:8002/health`.

## 4.2 Какие интерфейсные скриншоты нужны

- `Рисунок 16` — страница входа;
- `Рисунок 17` — dashboard;
- `Рисунок 18` — список клиентов;
- `Рисунок 19` — форма создания клиента;
- `Рисунок 20` — карточка клиента с найденными объектами;
- `Рисунок 21` — детальная карточка объекта.

### Что важно по скрину карточки клиента

На `Рисунке 20` должны быть видны:

- имя клиента;
- параметры поиска;
- две плашки `В подборке` и `Найдено`;
- найденные объекты;
- блок подборки;
- кнопка `Обновить поиск`.

Не надо обещать отдельный визуальный раздел истории запусков, потому что в текущем интерфейсе его нет.

## 4.3 Какие скриншоты базы данных нужны

- `Рисунок 22` — список таблиц;
- `Рисунок 23` — содержимое основных таблиц.

Для `Рисунка 23` лучше сделать два скрина:

1. `users`, `clients`, `client_search_profiles`;
2. `properties`, `client_found_properties`, `shortlist_items`, `share_messages`, `search_runs`.

## 4.4 Кодовые рисунки приложения В

### Рисунок В.1 – Авторизация риелтора и создание клиентской заявки

**Фрагмент 1.** Файл `apps/api/src/data/realtors.ts`.

Показать массив `seedRealtors`, чтобы были видны логины, имена и факт преднастроенного создания учетных записей.

Ориентир по строкам: область массива `seedRealtors` рядом со строками `12–47`.

**Фрагмент 2.** Файл `apps/api/src/routes/auth.ts`.

Показать обработчик `POST /login`, чтобы были видны:

- чтение логина;
- поиск пользователя;
- проверка пароля;
- выдача сессионного токена.

Ориентир по строкам: обработчик `POST /login` рядом со строками `22–51`.

**Фрагмент 3.** Файл `apps/api/src/routes/clients.ts`.

Показать функции `profileData`, `dbPayload` и обработчик `POST /clients`, чтобы были видны:

- подготовка поискового профиля;
- сбор объекта для записи в БД;
- создание клиента;
- автоматический запуск первого поиска.

Ориентир по строкам: функции `profileData` и `dbPayload` рядом со строками `51–89`, обработчик `POST /clients` рядом со строками `113–119`.

**Что должно получиться на рисунке В.1**

Составной code screenshot из 2–3 фрагментов, показывающий, как система формирует исходные учетные записи, выполняет вход пользователя и создает клиентскую заявку.

### Рисунок В.2 – Запуск поиска и сохранение результатов поиска

**Фрагмент 1.** Файл `apps/api/src/services/searchService.ts`.

Показать функцию `buildSearchRequest`, чтобы были видны преобразование бюджета, площади, районов и других параметров в запрос к поисковому сервису.

Ориентир по строкам: `77–105`.

**Фрагмент 2.** Файл `apps/api/src/services/searchService.ts`.

Показать функцию `runClientSearch`, чтобы были видны:

- создание `search_run`;
- перевод клиента в статус `searching`;
- вызов Python-сервиса;
- сохранение объектов в `properties`;
- сохранение связей в `client_found_properties`;
- фиксация итогового статуса.

Ориентир по строкам: `140–187`.

**Фрагмент 3.** Файл `apps/api/src/routes/clients.ts`.

Показать обработчик `POST /clients/:id/search`, чтобы было видно повторное выполнение поиска из карточки клиента.

Ориентир по строкам: `157–163`.

**Что должно получиться на рисунке В.2**

Составной code screenshot, показывающий, как параметры заявки переходят в поисковый запрос, а затем сохраняются как результаты поиска и история запусков.

### Рисунок В.3 – Нормализация объявления и расчет процента совпадения

**Фрагмент 1.** Файл `apps/search-service/app/adapters/base.py`.

Показать функцию `_parse_html`, чтобы было видно:

- разбор HTML через `BeautifulSoup`;
- объединение результатов из JSON-LD и видимых карточек;
- отбрасывание объектов без цены;
- ограничение результатов только городом Краснодаром.

Ориентир по строкам: `115–127`.

**Фрагмент 2.** Файл `apps/search-service/app/adapters/base.py`.

Показать функции `_item_from_json_ld`, `_normalize_title` и `_is_krasnodar_city`, чтобы были видны:

- извлечение цены, площади, комнатности, района;
- формирование краткого названия вида `1-к квартира, 40.6 м²`;
- исключение объектов из других городов Краснодарского края.

Ориентир по строкам: `_item_from_json_ld` — `158–239`, `_normalize_title` — `357–381`, `_is_krasnodar_city` — `520–529`.

**Фрагмент 3.** Файл `apps/search-service/app/services/matcher.py`.

Показать функцию `score_property`, чтобы были видны:

- начисление баллов по бюджету;
- начисление баллов по комнатности, площади и району;
- расчет итогового `matchScore`;
- формирование причин совпадения.

Ориентир по строкам: `24–100`.

**Что должно получиться на рисунке В.3**

Составной code screenshot, раскрывающий основную прикладную ценность системы: переход от сырого внешнего объявления к нормализованному объекту с вычисленным процентом совпадения.

### Рисунок В.4 – Формирование подборки, сообщения и сводных показателей

**Фрагмент 1.** Файл `apps/api/src/routes/clients.ts`.

Показать обработчики:

- `POST /clients/:id/shortlist/:propertyId`;
- `DELETE /clients/:id/shortlist/:propertyId`;
- `POST /clients/:id/share-message`;
- `POST /clients/:id/mark-sent`.

Ориентир по строкам: `188–236`.

**Фрагмент 2.** Файл `apps/api/src/services/messageService.ts`.

Показать функцию `buildShareMessage`, чтобы было видно:

- формирование приветствия;
- перечисление выбранных объектов;
- добавление цены, площади и ссылки;
- сборку итогового текста.

Ориентир по строкам: `39–74`.

**Фрагмент 3.** Файл `apps/api/src/routes/dashboard.ts`.

Показать обработчики `/summary` и `/activity`, чтобы было видно получение KPI и активности по дням.

Ориентир по строкам: `13–42`.

**Что должно получиться на рисунке В.4**

Составной code screenshot, показывающий завершающий этап работы с заявкой: формирование подборки, подготовку текста сообщения и расчет сводных показателей.

## 4.5 Скрины модульных тестов

### Рисунок 25 – Фрагмент программного кода модульных тестов нормализации объявлений

Брать из файла `apps/search-service/tests/test_normalization.py`.

На скрине должны быть видны тесты:

- `test_bad_apartment_title_is_replaced_with_structured_title`;
- `test_krasnodar_city_filter_rejects_other_krai_cities`;
- `test_krasnodar_city_filter_accepts_city_district`;
- `test_city_district_and_characteristics_are_extracted`.

Ориентир по строкам: `27–37`, `65–86`.

### Рисунок 26 – Фрагмент программного кода модульных тестов расчета процента совпадения

Брать из файла `apps/search-service/tests/test_matcher.py`.

На скрине должны быть видны тесты:

- `test_apartment_filters_keep_apartment_context`;
- `test_house_filters_keep_house_context`.

Ориентир по строкам: `15–67`.

### Рисунок 27 – Результат успешного запуска модульных тестов

Команда:

```bash
cd apps/search-service && .venv/bin/python -m unittest discover -s tests -v
```

На `Рисунке 27` должно быть видно:

- имена тестов;
- итог `OK`;
- число пройденных тестов.

---

# 5 Формулы, которые нужно вставить

## 5.1 Формула (1)

`C_{м²} = C / S`

**Где вставлять:** подраздел `2.6.2`.

**Расшифровка:**

- `C_{м²}` — стоимость одного квадратного метра;
- `C` — полная стоимость объекта;
- `S` — общая площадь объекта.

## 5.2 Формула (2)

`M = (Σ(w_i * k_i) / Σw_i) * 100%`

**Где вставлять:** подраздел `2.6.4`.

**Расшифровка:**

- `M` — итоговый процент совпадения;
- `w_i` — вес `i`-го критерия;
- `k_i` — коэффициент выполнения критерия.

## 5.3 Формула (3)

`k_{комм} = n_{совп} / n_{треб}`

**Где вставлять:** подраздел `2.6.4`.

**Расшифровка:**

- `k_{комм}` — коэффициент совпадения по коммуникациям;
- `n_{совп}` — число совпавших коммуникаций;
- `n_{треб}` — число требуемых коммуникаций.

---

# 6 Итоговый чек-лист

## 6.1 Что должно получиться в основном тексте

- `Рисунки 1–5` — use case;
- `Рисунок 6` — деятельность;
- `Рисунок 7` — состояния;
- `Рисунок 8` — компоненты;
- `Рисунки 9–10` — ER;
- `Рисунки 11–15` — блок-схемы алгоритмов;
- `Рисунки 16–21` — экранные формы;
- `Рисунки 22–23` — база данных;
- `Рисунок 24` — структура проекта;
- `Рисунки 25–27` — модульные тесты;
- `Таблицы 1–16` — все таблицы основной части.

## 6.2 Что должно получиться по приложениям

- `Приложение А` — таблицы `А.1–А.4`;
- `Приложение Б` — стиль;
- `Приложение В` — рисунки `В.1–В.4` и таблицы `В.1–В.4`;
- `Приложение Г` — таблицы `Г.1–Г.4`;
- `Приложение Д` — таблица `Д.1`;
- `Приложение Е` — таблица `Е.1`.

## 6.3 Практический смысл файла

Если основной `Диплом_структура_ПЗ.md` отвечает на вопрос, что писать в ПЗ, а `Приложения_ПЗ.md` отвечает на вопрос, что включать в приложения, то настоящий файл отвечает на вопрос, что именно нужно нарисовать, расписать и вставить, чтобы ПЗ собралась без пустых мест и противоречий.
