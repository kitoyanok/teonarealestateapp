# Приложение А

(обязательное)

**Техническое задание. Требования к программным модулям**

## 1 Требования к функциональным характеристикам

В программную систему `Тэона` должны входить следующие модули:

- модуль авторизации и ведения клиентских заявок;
- модуль поиска и обработки объявлений;
- модуль формирования подборки и сообщения;
- модуль аналитики и контроля состояния заявок.

Каждый модуль должен быть реализован в соответствии с назначением системы и обеспечивать совместную обработку данных о клиентах, параметрах поиска, найденных объектах недвижимости и результатах работы риелтора.

### 1.1 Требования к составу выполняемых функций модуля авторизации и ведения клиентских заявок

Модуль авторизации и ведения клиентских заявок должен быть предназначен для формирования, хранения, изменения и представления данных о риелторах и клиентах.

Основные функции модуля авторизации и ведения клиентских заявок должны состоять из:

- функции формирования исходных учетных записей риелторов;
- функции авторизации пользователя;
- функции формирования карточки клиента;
- функции ведения данных клиента;
- функции управления состоянием клиентской заявки.

Функция формирования исходных учетных записей риелторов должна решать следующие задачи:

- формирование исходного набора учетных записей риелторов;
- хранение логинов, имен и контактных данных пользователей;
- подготовку учетных данных к использованию в системе;
- обеспечение возможности входа в программу без ручного добавления новых пользователей.

Функция авторизации пользователя должна решать следующие задачи:

- проверку логина и пароля риелтора;
- предоставление доступа только существующим пользователям;
- формирование пользовательской сессии;
- предотвращение некорректного входа в систему.

Функция формирования карточки клиента должна решать следующие задачи:

- создание новой карточки клиента;
- ввод и сохранение контактных данных клиента;
- ввод и сохранение параметров поиска недвижимости;
- связывание карточки клиента с конкретным риелтором;
- автоматический запуск поиска после сохранения карточки.

Функция ведения данных клиента должна решать следующие задачи:

- хранение и изменение сведений о клиенте;
- хранение параметров поиска;
- представление сведений о клиенте и найденных объектах;
- сохранение истории действий по карточке клиента.

Функция управления состоянием клиентской заявки должна решать следующие задачи:

- поддержку состояний `new`, `searching`, `found`, `no_results`, `shortlist_ready`, `sent`, `error`;
- обеспечение переходов между состояниями в соответствии с выполненными действиями;
- предотвращение некорректных переходов;
- актуализацию состояния заявки после запуска поиска, формирования подборки и отправки сообщения.

### 1.2 Требования к составу выполняемых функций модуля поиска и обработки объявлений

Модуль поиска и обработки объявлений должен быть предназначен для автоматизированного формирования списка объектов недвижимости на основе параметров клиентской заявки.

Основные функции модуля поиска и обработки объявлений должны состоять из:

- функции анализа параметров поиска клиента;
- функции загрузки и первичного отбора объявлений;
- функции фильтрации объектов по городу Краснодару;
- функции нормализации характеристик объектов;
- функции расчета процента совпадения и ранжирования результатов.

Функция анализа параметров поиска клиента должна решать следующие задачи:

- определение параметров, влияющих на поиск объектов;
- выявление ограничений по бюджету, площади, комнатности и району;
- подготовку входных данных для поискового модуля;
- формирование запроса к внешним источникам.

Функция загрузки и первичного отбора объявлений должна решать следующие задачи:

- получение данных с внешних сайтов недвижимости;
- извлечение карточек объектов;
- отбор объявлений, содержащих обязательные данные;
- исключение неполных и некорректных результатов.

Функция фильтрации объектов по городу Краснодару должна решать следующие задачи:

- определение принадлежности объекта к городу Краснодару;
- исключение объявлений из других городов Краснодарского края;
- учет района, текста объявления и URL-адреса источника;
- предотвращение попадания нерелевантных объектов в выдачу.

Функция нормализации характеристик объектов должна решать следующие задачи:

- приведение названия объекта к единому виду;
- выделение площади, комнатности, цены и района;
- формирование структурированного описания объекта;
- очистку текстового описания и изображений.

Функция расчета процента совпадения и ранжирования результатов должна решать следующие задачи:

- сопоставление характеристик объекта с требованиями клиента;
- начисление баллов по ключевым критериям;
- расчет итогового значения `matchScore`;
- формирование причин совпадения и несоответствия;
- ранжирование найденных результатов.

### 1.3 Требования к составу выполняемых функций модуля формирования подборки и сообщения

Модуль формирования подборки и сообщения должен быть предназначен для отбора подходящих объектов недвижимости и подготовки текста сообщения для клиента.

Основные функции модуля формирования подборки и сообщения должны состоять из:

- функции формирования подборки объектов;
- функции ведения данных подборки;
- функции формирования текста сообщения;
- функции фиксации факта отправки подборки.

Функция формирования подборки объектов должна решать следующие задачи:

- выбор подходящих объектов из списка найденных результатов;
- сохранение выбранных объектов в подборке клиента;
- предотвращение дублирования объектов в подборке;
- обеспечение возможности удаления объекта из подборки.

Функция ведения данных подборки должна решать следующие задачи:

- представление текущего состава подборки;
- хранение связи между клиентом и выбранными объектами;
- предоставление данных для формирования итогового сообщения;
- обеспечение повторного редактирования подборки.

Функция формирования текста сообщения должна решать следующие задачи:

- подготовку краткого описания каждого выбранного объекта;
- объединение данных по объектам в единый текст;
- указание цены, площади, района и ссылки на источник;
- сохранение готового сообщения для последующего использования.

Функция фиксации факта отправки подборки должна решать следующие задачи:

- сохранение признака отправки;
- изменение состояния карточки клиента после отправки;
- исключение неоднозначности в текущем состоянии работы по заявке.

### 1.4 Требования к составу выполняемых функций модуля аналитики и контроля состояния заявок

Модуль аналитики и контроля состояния заявок должен быть предназначен для представления сводной информации о текущей работе риелтора.

Основные функции модуля аналитики и контроля состояния заявок должны состоять из:

- функции расчета сводных показателей;
- функции представления активности по заявкам;
- функции контроля текущих состояний карточек клиентов;
- функции представления аналитической информации.

Функция расчета сводных показателей должна решать следующие задачи:

- определение числа клиентов в работе;
- определение числа найденных объектов;
- определение числа объектов в подборках;
- определение числа подборок, готовых к отправке.

Функция представления активности по заявкам должна решать следующие задачи:

- группировку истории запусков поиска по дням;
- построение сводной активности по текущим карточкам;
- представление данных для визуального графика на dashboard.

Функция контроля текущих состояний карточек клиентов должна решать следующие задачи:

- выявление карточек в состоянии `error`;
- выявление карточек в состоянии `no_results`;
- выявление карточек, требующих повторного действия риелтора;
- обеспечение быстрого перехода к проблемным карточкам.

Функция представления аналитической информации должна решать следующие задачи:

- отображение KPI-показателей на главной странице;
- отображение активности по заявкам;
- представление короткой управленческой информации по текущему состоянию системы.

### 1.5 Требования к составу входных и выходных данных

#### 1.5.1 Требования к составу входных и выходных данных модуля авторизации и ведения клиентских заявок

Входные данные модуля авторизации и ведения клиентских заявок представлены в таблице А.1. Выходные данные модуля представлены в таблице А.2.

**Таблица А.1 – Входные данные модуля авторизации и ведения клиентских заявок**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Логин риелтора | Строковый | - | Обязательное поле |
| Пароль риелтора | Строковый | - | Обязательное поле |
| ФИО клиента | Строковый | - | Обязательное поле, не более 150 символов |
| Телефон клиента | Строковый | `+7XXXXXXXXXX` | Обязательное поле |
| Электронная почта клиента | Строковый | `name@example.com` | Необязательное поле |
| Тип недвижимости | Справочный | `Квартира` / `Дом` | Обязательное поле |
| Бюджет покупки | Числовой | Денежное значение | Значение не меньше 0 |
| Районы поиска | Справочный список | Название района | Допускается выбор нескольких значений |

**Таблица А.2 – Выходные данные модуля авторизации и ведения клиентских заявок**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Сессия пользователя | Составной | Данные авторизованного риелтора | Формируется при успешном входе |
| Карточка клиента | Составной | Набор сведений о клиенте | Формируется по сохраненным данным |
| Профиль поиска | Составной | Параметры поиска недвижимости | Привязан к карточке клиента |
| Статус клиентской заявки | Справочный | `new`, `searching`, `found`, `no_results`, `shortlist_ready`, `sent`, `error` | Должен соответствовать допустимым состояниям |

#### 1.5.2 Требования к составу входных и выходных данных модуля поиска и обработки объявлений

Входные данные модуля поиска и обработки объявлений представлены в таблице А.3. Выходные данные модуля представлены в таблице А.4.

**Таблица А.3 – Входные данные модуля поиска и обработки объявлений**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Параметры поиска клиента | Составной | Профиль поиска | Обязательный набор данных |
| HTML-страницы источников | Текстовый | HTML | Должны быть доступны для обработки |
| URL-адреса объявлений | Строковый | URL | Должны быть корректными |
| Текст объявлений | Текстовый | - | Должен поддерживать русский язык |

**Таблица А.4 – Выходные данные модуля поиска и обработки объявлений**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Перечень найденных объектов | Список | Карточки объектов | Включаются только релевантные результаты |
| Нормализованные характеристики объекта | Составной | Название, цена, площадь, комнаты, район | Формируются после обработки объявления |
| Процент совпадения | Числовой | Процентное значение | Диапазон от 0 до 100 |
| История запуска поиска | Составной | Статус, дата, число результатов | Формируется по каждому запуску |

#### 1.5.3 Требования к составу входных и выходных данных модуля формирования подборки и сообщения

Входные данные модуля формирования подборки и сообщения представлены в таблице А.5. Выходные данные модуля представлены в таблице А.6.

**Таблица А.5 – Входные данные модуля формирования подборки и сообщения**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Список найденных объектов | Список | Карточки объектов | Объекты должны быть привязаны к клиенту |
| Выбранные объекты | Список | Идентификаторы объектов | Не должны дублироваться |
| Данные клиента | Составной | Имя, контакты, канал связи | Используются при формировании сообщения |

**Таблица А.6 – Выходные данные модуля формирования подборки и сообщения**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Подборка объектов | Список | Выбранные карточки объектов | Формируется вручную |
| Текст сообщения | Текстовый | - | Формируется только при непустой подборке |
| Признак отправки подборки | Логический / статусный | Да / Нет | Устанавливается после подтверждения отправки |

#### 1.5.4 Требования к составу входных и выходных данных модуля аналитики и контроля состояния заявок

Входные данные модуля аналитики и контроля состояния заявок представлены в таблице А.7. Выходные данные модуля представлены в таблице А.8.

**Таблица А.7 – Входные данные модуля аналитики и контроля состояния заявок**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Карточки клиентов | Список | Набор данных по клиентам | Должны быть связаны с риелтором |
| Статусы заявок | Справочный | Допустимые состояния заявки | Используются для группировки |
| История запусков поиска | Список | Даты и результаты запусков | Используется для расчета активности |
| Подборки клиентов | Список | Данные о составе подборок | Используются для KPI |

**Таблица А.8 – Выходные данные модуля аналитики и контроля состояния заявок**

| Наименование данных | Тип данных | Формат данных | Ограничения |
|---|---|---|---|
| Число клиентов в работе | Числовой | Целое значение | Рассчитывается по текущим карточкам |
| Число найденных объектов | Числовой | Целое значение | Рассчитывается по результатам поиска |
| Число объектов в подборках | Числовой | Целое значение | Рассчитывается по составу подборок |
| Число подборок, готовых к отправке | Числовой | Целое значение | Рассчитывается по статусам заявок |
| Активность по заявкам | Составной | Значения по дням | Используется для графиков и KPI |

#### 1.5.5 Общие ограничения к данным

К данным, используемым программными модулями, должны предъявляться следующие общие ограничения:

- обязательные поля не должны сохраняться пустыми;
- денежные значения не должны быть отрицательными;
- URL-адреса источников должны быть корректными;
- текстовые поля должны поддерживать русский язык;
- результаты поиска должны ограничиваться городом Краснодаром;
- идентификаторы сущностей должны обеспечивать однозначную связь между клиентами, объектами, подборками и сообщениями.

## 1.6 Требования к надежности

В системе `Тэона` должны быть обеспечены:

- сохранение данных после перезапуска приложения;
- сохранение карточки клиента при ошибке поиска;
- возможность повторного запуска поиска;
- корректная фиксация ошибки в истории запусков;
- сохранение данных в базе `PostgreSQL` с использованием постоянного хранилища.

## 1.7 Условия эксплуатации

Для обеспечения надежной работы системы и сохранности данных рабочее место должно иметь:

- `Docker Desktop`;
- `Node.js`;
- `Visual Studio Code`;
- `TablePlus` или аналогичное средство просмотра базы данных;
- современный web-браузер;
- устойчивый доступ к локальным сервисам `localhost:5002`, `localhost:5003` и `localhost:8002`.

## 1.8 Требования к информационной совместимости

Информационная совместимость должна обеспечивать:

- согласованность структуры данных между frontend, backend API, поисковым сервисом и базой данных;
- хранение данных о клиентах, параметрах поиска, найденных объектах, подборках и истории сообщений;
- возможность использования данных одного модуля при выполнении функций других модулей;
- единое представление характеристик объекта недвижимости после нормализации.

---

# Приложение Б

(обязательное)

**Руководство по стилю**

Данное руководство по стилю определяет визуальную идентичность программы `Тэона`. Данное руководство описывает использование цветов, шрифтов и основных элементов интерфейса.

Приложение должно быть выполнено в минималистическом стиле. Интерфейс ориентирован на ежедневную работу риелтора, поэтому в нем не должны использоваться декоративные иллюстрации, перегруженные композиции и избыточные визуальные эффекты.

Основной шрифт: `Inter`.

Основные цвета:  
HEX: `#FD6000`  
HEX: `#E95700`  
HEX: `#3D3D3D`  
HEX: `#898989`  
HEX: `#FAFAFA`

Вторичные цвета:  
HEX: `#F1F1F1`  
HEX: `#FFFFFF`  
HEX: `#E7E7E7`  
HEX: `#FFF1E8`  
HEX: `#FFD2B8`

Основное действие должно выделяться оранжевой кнопкой с белым текстом. При наведении используется белый фон и оранжевая обводка. Второстепенные действия оформляются белой кнопкой со светло-серой границей.

Карточки объектов недвижимости должны содержать название объекта, основные характеристики, район, цену, процент совпадения и кнопки работы с подборкой. Формы ввода должны быть выдержаны в едином стиле и содержать только необходимые для работы поля.

---

# Приложение В

(обязательное)

**Программный код модулей**

Программный код представлен в контексте среды разработки `Visual Studio Code`, контейнерного окружения `Docker Desktop` и сервисной архитектуры проекта `Тэона`.

## 1 Модуль авторизации и ведения клиентских заявок

### 1.1 Назначение модуля

Модуль авторизации и ведения клиентских заявок отвечает за работу с учетными записями риелторов, вход пользователя в систему, создание карточки клиента и сохранение параметров поиска.

### 1.2 Функции модуля

Функции модуля представлены в таблице В.1.

**Таблица В.1 – Функции модуля авторизации и ведения клиентских заявок**

| Функция | Задача функции | Входные данные | Выходные данные |
|---|---|---|---|
| `seedRealtors` | Формирование преднастроенных учетных записей риелторов | Имена, логины, контакты, пароли | Набор учетных записей |
| `POST /login` | Проверка учетных данных пользователя | Логин, пароль | Сессия пользователя |
| `profileData` | Подготовка профиля поиска | Данные формы клиента | Профиль поиска |
| `dbPayload` | Подготовка данных к сохранению | Данные клиента и профиля | Структура для записи в БД |
| `POST /clients` | Создание карточки клиента и запуск поиска | Данные клиента, параметры поиска | Карточка клиента, старт поиска |

`Рисунок В.1 – Авторизация риелтора и создание клиентской заявки`

На рисунке В.1 следует представить составной фрагмент файлов `apps/api/src/data/realtors.ts`, `apps/api/src/routes/auth.ts` и `apps/api/src/routes/clients.ts`, на котором должны быть видны формирование учетных записей, проверка учетных данных пользователя и подготовка профиля поиска клиента.

## 2 Модуль поиска и обработки объявлений

### 2.1 Назначение модуля

Модуль поиска и обработки объявлений отвечает за запуск поиска по параметрам клиента, обращение к внешним источникам и сохранение найденных результатов в базе данных.

### 2.2 Функции модуля

Функции модуля представлены в таблице В.2.

**Таблица В.2 – Функции модуля поиска и обработки объявлений**

| Функция | Задача функции | Входные данные | Выходные данные |
|---|---|---|---|
| `runClientSearch` | Запуск поиска и сохранение результатов | Идентификатор клиента, параметры поиска | Результат запуска поиска |
| `POST /clients/:id/search` | Повторный запуск поиска | Идентификатор клиента | Новая история запуска |
| Сохранение `search_runs` | Фиксация истории поиска | Статус, дата, число объектов | Запись истории запуска |
| Сохранение `client_found_properties` | Сохранение найденных объектов | Найденные объекты | Результаты в БД |

`Рисунок В.2 – Запуск поиска и сохранение результатов поиска`

На рисунке В.2 следует представить составной фрагмент файлов `apps/api/src/routes/clients.ts` и `apps/api/src/services/searchService.ts`, на котором должны быть видны повторный запуск поиска, обращение к поисковому сервису и сохранение результатов в таблицах базы данных.

## 3 Модуль нормализации и ранжирования объявлений

### 3.1 Назначение модуля

Модуль нормализации и ранжирования объявлений отвечает за географическую фильтрацию по городу Краснодару, приведение данных объявления к единому виду и расчет процента совпадения.

### 3.2 Функции модуля

Функции модуля представлены в таблице В.3.

**Таблица В.3 – Функции модуля нормализации и ранжирования объявлений**

| Функция | Задача функции | Входные данные | Выходные данные |
|---|---|---|---|
| `_is_krasnodar_city` | Исключение объектов других городов Краснодарского края | Текст объявления, адрес, URL | Решение о допуске объекта |
| `_normalize_title` | Формирование корректного названия объекта | Сырое название, характеристики объекта | Нормализованный заголовок |
| Извлечение характеристик | Выделение района, площади, цены и комнатности | Текст объявления | Структурированные данные |
| `score_property` | Расчет процента совпадения | Параметры клиента, характеристики объекта | `matchScore`, причины совпадения |

`Рисунок В.3 – Нормализация объявления и расчет процента совпадения`

На рисунке В.3 следует представить составной фрагмент файлов `apps/search-service/app/adapters/base.py` и `apps/search-service/app/services/matcher.py`, на котором должны быть видны географическая фильтрация, нормализация заголовка и расчет процента совпадения.

## 4 Модуль формирования подборки, сообщения и аналитики

### 4.1 Назначение модуля

Модуль формирования подборки, сообщения и аналитики отвечает за ручной отбор объектов, формирование сообщения и расчет сводных показателей.

### 4.2 Функции модуля

Функции модуля представлены в таблице В.4.

**Таблица В.4 – Функции модуля формирования подборки, сообщения и аналитики**

| Функция | Задача функции | Входные данные | Выходные данные |
|---|---|---|---|
| `POST /clients/:id/shortlist/:propertyId` | Добавление объекта в подборку | Идентификатор клиента, идентификатор объекта | Запись в подборке |
| `DELETE /clients/:id/shortlist/:propertyId` | Удаление объекта из подборки | Идентификатор клиента, идентификатор объекта | Обновленная подборка |
| `buildShareMessage` | Формирование текста сообщения | Данные клиента, объекты подборки | Готовый текст сообщения |
| `POST /clients/:id/mark-sent` | Фиксация факта отправки | Идентификатор клиента | Новый статус заявки |
| `dashboardSummary`, `dashboardActivity` | Расчет KPI и активности | Данные клиентов и историй поиска | Сводные показатели |

`Рисунок В.4 – Формирование подборки, сообщения и сводных показателей`

На рисунке В.4 следует представить составной фрагмент файлов `apps/api/src/routes/clients.ts`, `apps/api/src/services/messageService.ts` и `apps/api/src/routes/dashboard.ts`, на котором должны быть видны операции с подборкой, генерация текста сообщения и получение KPI-показателей.

---

# Приложение Г

(обязательное)

**Тест-кейсы**

**Таблица Г.1 – Тест-кейс на создание карточки клиента и автоматический запуск поиска**

| Название | Описание |
|---|---|
| Test Case # | `TC_INT_01` |
| Приоритет теста | Высокий |
| Название тестирования/Имя | Создание карточки клиента с корректными параметрами поиска |
| Резюме испытания | Проверяется корректность создания карточки клиента и возможность автоматического запуска поиска объектов недвижимости |
| Шаги тестирования | Действие №1. Авторизоваться в системе под существующей учетной записью риелтора.<br>Действие №2. Перейти в раздел создания нового клиента.<br>Действие №3. Ввести ФИО клиента и номер телефона.<br>Действие №4. Выбрать тип недвижимости и заполнить основные параметры поиска.<br>Действие №5. Нажать кнопку сохранения карточки клиента. |

**Продолжение таблицы Г.1**

| Название | Описание |
|---|---|
| Данные тестирования | ФИО клиента: `Иван Иванов`. Телефон: `+79180000000`. Тип недвижимости: квартира. Бюджет: `до 8 000 000`. Районы: `Прикубанский`, `Центральный`. Комнатность: `1–2`. |
| Ожидаемый результат | Карточка клиента сохраняется. Поиск запускается автоматически. В карточке клиента отображается статус обработки и затем найденные объекты. |
| Фактический результат | Карточка клиента сохраняется. Поиск запускается автоматически. В карточке клиента отображается статус обработки и затем найденные объекты. |
| Предпосылки | Программа `Тэона` запущена. Пользователь авторизован. Поисковый сервис и база данных доступны. |
| Постусловия | В таблицах `clients`, `client_search_profiles`, `search_runs` и `client_found_properties` появляются новые записи. |
| Статус (Pass/Fail) | Pass |
| Комментарии | - |

**Таблица Г.2 – Тест-кейс на формирование подборки и текста сообщения**

| Название | Описание |
|---|---|
| Test Case # | `TC_INT_02` |
| Приоритет теста | Высокий |
| Название тестирования/Имя | Формирование подборки объектов и текста сообщения для клиента |
| Резюме испытания | Проверяется совместная работа карточки клиента, модуля подборки и модуля формирования текста сообщения |
| Шаги тестирования | Действие №1. Открыть карточку клиента с уже найденными объектами.<br>Действие №2. Добавить подходящие объекты в подборку.<br>Действие №3. Перейти к блоку подборки.<br>Действие №4. Нажать кнопку формирования текста сообщения.<br>Действие №5. Просмотреть сформированный текст. |

**Продолжение таблицы Г.2**

| Название | Описание |
|---|---|
| Данные тестирования | Существующая карточка клиента с не менее чем тремя найденными объектами. В подборку добавляются два объекта. |
| Ожидаемый результат | Добавленные объекты отображаются в подборке. Система формирует текст сообщения с перечислением выбранных объектов и сохраняет его в истории сообщений. |
| Фактический результат | Добавленные объекты отображаются в подборке. Система формирует текст сообщения с перечислением выбранных объектов и сохраняет его в истории сообщений. |
| Предпосылки | Программа `Тэона` запущена. Существует карточка клиента со статусом `found`. В таблице `client_found_properties` присутствуют найденные объекты. |
| Постусловия | В таблицах `shortlist_items` и `share_messages` появляются новые записи. |
| Статус (Pass/Fail) | Pass |
| Комментарии | - |

**Таблица Г.3 – Тест-кейс на блокировку формирования сообщения при пустой подборке**

| Название | Описание |
|---|---|
| Test Case # | `TC_INT_03` |
| Приоритет теста | Высокий |
| Название тестирования/Имя | Проверка запрета формирования сообщения при отсутствии объектов в подборке |
| Резюме испытания | Проверяется корректность обработки ситуации, при которой пользователь пытается сформировать сообщение без выбранных объектов |
| Шаги тестирования | Действие №1. Открыть карточку клиента, у которой подборка пуста.<br>Действие №2. Перейти к блоку подборки.<br>Действие №3. Нажать кнопку формирования текста сообщения. |

**Продолжение таблицы Г.3**

| Название | Описание |
|---|---|
| Данные тестирования | Карточка клиента без записей в таблице `shortlist_items`. |
| Ожидаемый результат | Система не формирует сообщение. Пользователю выводится предупреждение `Подборка пока пустая`. Состояние карточки клиента не изменяется. |
| Фактический результат | Система не формирует сообщение. Пользователю выводится предупреждение `Подборка пока пустая`. Состояние карточки клиента не изменяется. |
| Предпосылки | Программа `Тэона` запущена. Существует карточка клиента с найденными объектами, но без сформированной подборки. |
| Постусловия | В таблице `share_messages` новые записи не появляются. |
| Статус (Pass/Fail) | Pass |
| Комментарии | - |

**Таблица Г.4 – Модульные тесты поискового сервиса**

| Название | Описание |
|---|---|
| Test Case # | `TC_UNIT_01` |
| Приоритет теста | Высокий |
| Название тестирования/Имя | Проверка нормализации и сопоставления объявлений |
| Резюме испытания | Проверяется корректность автоматических тестов `test_normalization.py` и `test_matcher.py` |
| Шаги тестирования | Действие №1. Перейти в каталог `apps/search-service`.<br>Действие №2. Выполнить команду `.venv/bin/python -m unittest discover -s tests -v`.<br>Действие №3. Дождаться завершения тестового прогона. |

**Продолжение таблицы Г.4**

| Название | Описание |
|---|---|
| Данные тестирования | Исходный код поискового сервиса и набор тестов `test_normalization.py`, `test_matcher.py`. |
| Ожидаемый результат | Все тесты завершаются успешно. В консоли отображается итог `OK`. |
| Фактический результат | Все тесты завершаются успешно. В консоли отображается итог `OK`. |
| Предпосылки | Установлены зависимости поискового сервиса. |
| Постусловия | Подтверждается корректность работы алгоритмов нормализации и расчета `matchScore`. |
| Статус (Pass/Fail) | Pass |
| Комментарии | - |

`Рисунок Г.1 – Результат успешного прохождения модульных тестов`

---

# Приложение Д

(обязательное)

**Руководство пользователя. Выполнение программы и сообщения пользователю**

## 1 Выполнение программы

Для начала работы пользователь открывает web-приложение `Тэона`. После запуска открывается страница входа в систему. Для входа в программу риелтор вводит логин и пароль одной из преднастроенных учетных записей.

После успешной авторизации открывается главная страница, на которой отображаются сводные показатели по текущим клиентским заявкам. Пользователь может перейти к списку клиентов, создать новую карточку клиента или открыть уже существующую карточку.

Для создания новой карточки пользователь переходит в раздел создания клиента. На форме указываются основные сведения о клиенте и параметры поиска недвижимости. После сохранения карточки система автоматически запускает поиск объектов.

В карточке клиента отображаются найденные объекты недвижимости, их цена, район, основные характеристики и процент совпадения. Пользователь может открыть подробную информацию об объекте и добавить выбранный объект в подборку.

После формирования подборки пользователь переходит к блоку сообщения. При наличии выбранных объектов система формирует текст сообщения для клиента. После фактической отправки риелтор может отметить подборку как отправленную.

После завершения работы пользователь закрывает программу стандартным способом средствами браузера и операционной системы.

## 2 Сообщения пользователю

При работе с программой пользователь может получать информационные сообщения и предупреждения. Основные сообщения пользователю приведены в таблице Д.1.

**Таблица Д.1 – Основные сообщения пользователю**

| Сообщение | Причина появления | Действие пользователя |
|---|---|---|
| `Неверный логин или пароль` | Указаны некорректные данные авторизации | Повторно ввести логин и пароль |
| `Подборка пока пустая` | Пользователь пытается сформировать сообщение без выбранных объектов | Добавить хотя бы один объект в подборку |
| Сообщение о незаполненных обязательных полях | Не заполнены поля формы клиента | Заполнить обязательные поля и повторить сохранение |
| Состояние `Поиск не дал результатов` | По параметрам клиента не найдено подходящих объектов | Скорректировать параметры поиска и повторить запуск |
| Состояние `Ошибка поиска` | Поисковый сервис недоступен или завершился ошибкой | Повторить запуск после устранения причины |
| Отсутствие обновления данных на странице | Пользователь работает с устаревшим состоянием экрана | Обновить карточку клиента или повторно открыть страницу |

---

# Приложение Е

(обязательное)

**Руководство программиста. Характеристика программы. Обращение к программе. Сообщения.**

## 1 Характеристика программы

Программная система `Тэона` реализована как web-приложение с разделением на клиентскую часть, backend API, отдельный поисковый сервис и реляционную базу данных `PostgreSQL`. Основной проект построен по модульному принципу и включает каталоги `apps/web`, `apps/api` и `apps/search-service`.

В программе выделены четыре функциональных направления: модуль авторизации и ведения клиентских заявок, модуль поиска и обработки объявлений, модуль формирования подборки и сообщения, модуль аналитики и контроля состояния заявок.

### 1.1 Модуль авторизации и ведения клиентских заявок

Модуль отвечает за проверку учетных данных риелтора, создание карточки клиента, сохранение параметров поиска и повторный запуск поиска. Основными функциями модуля являются формирование исходных учетных записей, обработка входа пользователя, подготовка профиля поиска и сохранение карточки клиента.

### 1.2 Модуль поиска и обработки объявлений

Модуль отвечает за обращение к внешним источникам недвижимости, фильтрацию результатов по городу Краснодару, нормализацию характеристик объявлений и расчет процента совпадения. Основными функциями модуля являются запуск поиска, сохранение истории запусков, нормализация названий объектов, определение района и ранжирование результатов.

### 1.3 Модуль формирования подборки и сообщения

Модуль отвечает за ручной отбор объектов, формирование подборки, генерацию итогового текста сообщения и фиксацию факта отправки. Основными функциями модуля являются добавление и удаление объектов из подборки, формирование текста сообщения и изменение состояния клиентской заявки.

### 1.4 Модуль аналитики и контроля состояния заявок

Модуль отвечает за расчет сводных показателей по текущей работе риелтора. Основными функциями модуля являются расчет числа клиентов в работе, найденных объектов, объектов в подборках, готовых к отправке подборок и формирование активности по заявкам.

## 2 Обращение к программе

Для обращения к программе необходимо открыть корень проекта `realestate` в среде `Visual Studio Code`. После открытия проекта следует проверить наличие файла `.env`, доступность `Docker Desktop` и возможность подключения к базе данных `PostgreSQL`.

Подготовка к запуску выполняется в следующем порядке:

- открыть корневой каталог проекта в `Visual Studio Code`;
- при необходимости установить Node.js-зависимости командой `npm install`;
- запустить `Docker Desktop`;
- выполнить команду `npm run prod:up`;
- дождаться запуска контейнеров `postgres`, `search-service`, `api` и `web`;
- открыть в браузере адрес `http://localhost:5002`.

Для просмотра содержимого базы данных рекомендуется использовать `TablePlus`. Для этого необходимо подключиться к локальному экземпляру `PostgreSQL`, используя параметры из файла `docker-compose.yml`.

Для остановки программного продукта используется команда `npm run prod:down`. Для просмотра журналов контейнеров используется команда `npm run prod:logs`. Для полного сброса базы данных используется команда `npm run prod:reset-db`.

Модульные тесты запускаются командой `cd apps/search-service && .venv/bin/python -m unittest discover -s tests -v`. Указанные тесты проверяют корректность нормализации объявлений и расчета процента совпадения без необходимости ручного открытия интерфейса.

## 3 Сообщения

При запуске и сопровождении программы программист или системный администратор может получать служебные сообщения. Основные сообщения приведены в таблице Е.1.

**Таблица Е.1 – Основные служебные сообщения программы**

| Сообщение или ситуация | Возможная причина | Рекомендуемое действие |
|---|---|---|
| Контейнер `postgres` не запускается | Порт занят или поврежден том хранения | Проверить занятость порта `5432`, при необходимости выполнить `npm run prod:reset-db` |
| Контейнер `api` не проходит health-check | База данных или search-service недоступны | Проверить логи контейнеров и доступность зависимых сервисов |
| Контейнер `search-service` не проходит health-check | Ошибка Python-зависимостей или проблемы с запуском сервиса | Проверить логи контейнера `search-service` |
| Модульные тесты завершаются ошибкой | Нарушена логика нормализации или сопоставления | Проверить файлы `test_normalization.py` и `test_matcher.py` |
| В приложении не отображаются данные клиентов | Ошибка подключения frontend к API или проблема с авторизацией | Проверить доступность `http://localhost:5003/api/health` и корректность cookie-сессии |
| Повторный запуск контейнеров не сохраняет данные | Ошибка в работе постоянного тома PostgreSQL | Проверить подключение тома `estateflow-postgres` в `docker-compose.yml` |
